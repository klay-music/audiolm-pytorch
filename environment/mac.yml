name: audiolm

channels:
  - defaults
  - pytorch

dependencies:
  - pip
  - python=3.10
  - pytorch::pytorch=1.12.1
  - pytorch::torchaudio=0.12.1
  - pytorch::torchvision=0.13.1

  - pip:
    - -e '..[code-style, tests, type-check, torch]'
